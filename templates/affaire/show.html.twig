{% extends 'base.html.twig' %}

{% block title %}Affaire{% endblock %}

{% block body %}
    <h1>Affaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ affaire.id }}</td>
            </tr>
            <tr>
                <th>Nom français</th>
                <td>{{ affaire.nom_francais }}</td>
            </tr>
            <tr>
                <th>Nom Basque</th>
                <td>{{ affaire.nom_basque }}</td>
            </tr>
            <tr>
                <th>Type Affaire</th>
                <td>{{ affaire.type_affaire.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('affaire_index') }}">back to list</a>

    <a href="{{ path('affaire_modification', {'id': affaire.id}) }}">edit</a>

    {{ include('affaire/_delete_form.html.twig') }}
{% endblock %}
