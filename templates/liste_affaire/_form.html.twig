{{ form_start(form) }}

<div class="container">
	<div class="row">
		<div class="col">
			<div>
				{{ form_label(form.nom_francais,"Nom français",{'errors': false})}}
				{{ form_widget(form.nom_francais) }}
			</div>
			<div>
				{{ form_label(form.nom_basque,"Nom basque",{'errors': false})}}
				{{ form_widget(form.nom_basque) }}
			</div>
		</div>
		<div class="col">
			<div>
				{{ form_label(form.affaire,"Contenu de la liste",{'errors': false})}}
				{{ form_widget(form.affaire) }}
			</div>
		</div>
	</div>

	<br>
</div>

<button type="submit" class="btn btn-primary">Enregistrer !</button>

{{ form_end(form) }}
