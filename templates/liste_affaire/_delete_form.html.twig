<form method="post" action="{{ path('liste_affaire_suppression', {'id': liste_affaire.id}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ liste_affaire.id) }}">
    <button class="btn btn-danger">Supprimer </button>
</form>
