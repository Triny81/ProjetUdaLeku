{% extends 'base.html.twig' %}

{% block title %} Liste des séjours {% endblock %}

{% block body %}
    <h1> Gestion des séjours </h1>
	
	<a class="btn btn-success" href="{{ path('sejour_creation') }}" role="button"> Créer </a>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Séjour</th>
                <th>Date de début</th>
                <th>Date de fin</th>
                <th>N° ministre</th>
                <th>Coût</th>
                <th>Que voulez-vous faire ?</th>
            </tr>
        </thead>
        <tbody>
        {% for sejour in sejours %}
            <tr>
                <td>{{ sejour.id }}</td>
                <td>{{ sejour.nom }}</td>
                <td>{{ sejour.dateDebut ? sejour.dateDebut|date('Y-m-d') : '' }}</td>
                <td>{{ sejour.dateFin ? sejour.dateFin|date('Y-m-d') : '' }}</td>
                <td>{{ sejour.numMinistre }}</td>
                <td>{{ sejour.cout }}</td>
                <td>
                    <a class="btn btn-primary" href="{{ path('sejour_consultation', {'id': sejour.id}) }}"> Consulter </a>
                    <a class="btn btn-primary" href="{{ path('sejour_modification', {'id': sejour.id}) }}"> Modifier </a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table> 

   {# <a href="{{ path('sejour_creation') }}">Create new</a> #}
{% endblock %}
